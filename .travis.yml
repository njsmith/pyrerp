language: python
python:
  - 2.7
before_install:
  - sudo apt-get -qq build-dep python-scipy
  - pip install --use-mirrors --upgrade scipy
install:
  - true
script:
  - python test-script.py || true
  - python test-script2.py || true
  - PYRERP_FORCE_NO_WARNINGS=1 python -c 'import pyrerp.test_foo; pyrerp.test_foo.test_foo()' || true
  - python -c 'import pyrerp.test_foo; pyrerp.test_foo.test_foo()' || true
  - PYRERP_FORCE_NO_WARNINGS=1 python -c 'import pyrerp.test_bar' || true
  - python -c 'import pyrerp.test_bar' || true
  - PYRERP_FORCE_NO_WARNINGS=1 python -c 'import pyrerp.test_baz' || true
  - python -c 'import pyrerp.test_baz' || true
notifications:
  email:
    - njs@pobox.com
